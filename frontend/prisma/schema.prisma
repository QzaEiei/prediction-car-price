generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model TestPing {
  id        Int      @id @default(autoincrement())
  message   String
  createdAt DateTime @default(now())
}